'use client';
import { FormEvent, useEffect, useState } from 'react';
import AdminShell from '../components/AdminShell';
type Student = { id: string; fullName: string; studentCode: string; wallet: { balance: string; currency: string } | null };
const api = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:4000/api/v1';
export default function Wallets() { const [students, setStudents] = useState<Student[]>([]); const [message, setMessage] = useState(''); const load = async () => { const response = await fetch(`${api}/students`, { headers: { authorization: `Bearer ${localStorage.getItem('taazur_token')}` } }); if (response.status === 401) return location.assign('/login'); const data: { students?: Student[] } = await response.json(); setStudents(Array.isArray(data.students) ? data.students : []); }; useEffect(() => { void load(); }, []); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const form = e.currentTarget; const response = await fetch(`${api}/wallets/top-up`, { method: 'POST', headers: { authorization: `Bearer ${localStorage.getItem('taazur_token')}`, 'content-type': 'application/json' }, body: JSON.stringify(Object.fromEntries(new FormData(form))) }); if (!response.ok) return setMessage('تعذر شحن المحفظة.'); form.reset(); setMessage('تم شحن المحفظة وتسجيل العملية في دفتر الأستاذ.'); void load(); } return <AdminShell><header><div><h1>شحن المحافظ</h1><a href="/students">← الطلاب</a></div></header><form className="entry" onSubmit={submit}><select name="studentId" required defaultValue=""><option value="" disabled>اختر الطالب</option>{students.map(s => <option key={s.id} value={s.id}>{s.fullName} — {s.studentCode}</option>)}</select><input name="amount" type="number" min="0.01" step="0.01" placeholder="قيمة الشحن بالريال" required /><button>شحن الرصيد</button></form>{message && <p role="status">{message}</p>}<table><thead><tr><th>الطالب</th><th>الرمز</th><th>الرصيد الحالي</th></tr></thead><tbody>{students.map(s => <tr key={s.id}><td>{s.fullName}</td><td>{s.studentCode}</td><td>{s.wallet ? `${s.wallet.balance} ${s.wallet.currency}` : '—'}</td></tr>)}</tbody></table></AdminShell>; }
