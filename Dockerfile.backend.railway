FROM node:22-alpine

WORKDIR /app

COPY backend/package*.json ./
RUN npm install

COPY backend ./

# Prisma validates the URL while generating its client. Railway replaces this at runtime.
ENV DATABASE_URL="mysql://placeholder:placeholder@mysql:3306/taazur"
RUN npx prisma generate

CMD ["npm", "run", "start:deploy"]
